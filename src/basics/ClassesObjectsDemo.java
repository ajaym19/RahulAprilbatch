package basics;

public class ClassesObjectsDemo {

	/*
	 * Object:
	 * 
	 * is anything which you can see
	 * laptop, mobile, PC, tablet, pen, pencil, books
	 * 
	 * 
	 * table >> pen, pencil, laptop
	 * 
	 * Each and every Object has some properties/attributes and behaviors:
	 * Pen: 
	 * Attributes: company name, logo, length, weight, color, type
	 * Behavior: write, scribble
	 * 
	 * Laptop:
	 * Attributes: screensize, brand, logo, weight, color, size, ram, rom, 
	 * Behavior: touch, play, write programs, watch, create, 
	 * 
	 * Human Being:
	 * Attributes: name, age, color, height, weight, gender, nationality, pan, bld grp, 
	 * Behavior: running, writing, walking, thinking, reading, crying, sleeping , eating
	 *  
	 * Class:
	 * collection of similar object is a class
	 * 
	 * each student will be an object
	 * Students
	 * 
	 * Attributes in JAVA are handled by variables
	 * Behavior in JAVA are handled by methods
	 * 
	 * 
	 */
}
